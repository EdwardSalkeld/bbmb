# Optional environment overrides loaded by systemd.
# Copy to /etc/bbmb/bbmb.env if needed.
#
# Default ports are TCP 9876 and metrics 9877.
# To change them, add `--port=<port>` and/or `--metrics-port=<port>`
# to the `ExecStart=` line in a systemd override.
